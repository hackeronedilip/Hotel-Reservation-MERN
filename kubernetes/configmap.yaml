apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-config
data:
  PORT: "5555"
  MONGO_URI: "mongodb+srv://${process.env.DB_USER}:${process.env.DB_PASSWORD}@devopsdilip.dudrh.mongodb.net/?retryWrites=true&w=majority&appName=DevopsDilip"
